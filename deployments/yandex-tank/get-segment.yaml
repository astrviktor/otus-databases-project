phantom:
  enabled: true
#  package: yandextank.plugins.Phantom
  address: 10.5.0.2:8888
  timeout: 60s
  header_http: "1.1"
  headers:
    - "[Host: 10.5.0.2:8888]"
    - "[User-Agent: Tank]"
    - "[Connection: close]"
  uris:
    - /segment
  #  ammofile: /var/loadtest/get.txt
#  ammo_type: uriget
#  writelog: 0
  load_profile:
    load_type: rps
    schedule: line(10, 10, 1m)
#    schedule: const(10, 1m)
#  header_http: "1.1"
#  uris:
#    - "/segment/10000"
console:
  enabled: true # enable console output
telegraf:
  enabled: false # let's disable telegraf monitoring for the first time
#uploader:
#  enabled: false
